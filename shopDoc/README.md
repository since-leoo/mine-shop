# Mine Shop 文档中心

> 本目录承载官方站点内容，使用 [VitePress](https://vitepress.dev/) 构建，覆盖指南、架构、核心设计与 API 参考。

## 快速访问

- 🧭 **站点入口**：`shopDoc/index.md`（主页配置、Feature 展示、CTA）
- 📚 **指南**：`/guide/` – 安装、配置、整体介绍
- 🏗️ **架构**：`/architecture/` – DDD、分层、设计模式
- ⚙️ **核心能力**：`/core/` – 订单、库存、支付等内部实现
- 🔌 **API**：`/api/` – Admin、Frontend、Geo、Auth 等接口清单

## 本地运行

```bash
cd shopDoc

# 安装依赖（推荐 Node 20+ / npm 或 pnpm）
npm install

# 启动开发服务器（默认 http://localhost:5173）
npm run docs:dev

# 生成静态站点
npm run docs:build

# 预览构建结果
npm run docs:preview
```

## 目录结构

```
shopDoc/
├── index.md            # 站点首页（Hero / Features）
├── README.md           # 当前说明
├── START.md            # 快速启动备忘
├── guide/              # 入门与配置
│   ├── index.md        # 项目介绍
│   ├── installation.md # 安装部署
│   └── configuration.md# 配置项说明
├── architecture/       # 架构设计
│   ├── ddd.md
│   ├── layers.md
│   └── patterns.md
├── core/               # 核心模块
│   ├── order-design.md
│   ├── stock-management.md
│   └── payment.md
└── api/                # API 参考
    ├── index.md
    ├── admin.md
    ├── frontend.md
    └── auth.md
```

## 写作约定

- **面向官方读者**：保持产品级语气，突出价值、能力与落地指南。
- **同步代码**：新增模块（如 Geo 地址库、地区接口、会员驾驶舱）需在相关章节更新。
- **示例即代码**：示例请求/响应、命令、脚本务必与 `main` 分支代码一致。
- **多语言可扩展**：必要时预留英文标题或术语，方便未来切换语言版本。

## 贡献流程

1. Fork 仓库并拉取最新主分支。
2. 在 `shopDoc` 下创建或更新对应 Markdown。
3. 启动 `npm run docs:dev` 自查排版、链接、代码块高亮。
4. 提交 PR，描述涉及的模块与对应代码变更。

## 常用命令速查

| 命令 | 说明 |
| ---- | ---- |
| `npm run docs:dev` | 启动本地预览 |
| `npm run docs:build` | 生成静态站点 |
| `npm run docs:preview` | 构建后本地预览 |
| `npm run lint` *(可选)* | 若需要对 Markdown/TS 做额外校验 |

## 关联资源

- 主仓库：`/mnt/d/PhpstormProjects/mine-shop`
- 前端应用：`web/`（Vue 3 + Element Plus + MineAdmin）
- 后端服务：`app/`（Hyperf 3 + DDD）
- Geo 命令：`php bin/hyperf.php mall:sync-regions`

如需新增专题（例：DevOps、监控、FAQ），请在 `guide/` 或 `core/` 下按模块划分并更新首页导航。*** End Patch
